# Market Χ Project Status

Date: 2026-09-14
Branch: `market-chi-architecture`
Stage: P0-D / P0-Q

## Scientific direction

The project no longer begins by assuming a damped oscillator. Uppercase Χ is the broader reconstructed market stability architecture. Lowercase χ is emitted only when a licensed local or modal second-order factor supports it.

## Current executable path

`native series -> AR0/AR1/AR2 qualification scaffold -> pole inspection -> χ if licensed -> otherwise refusal`

This is deliberately narrow. It creates a production-callable refusal path and a known-truth test surface before larger model families are added.

## Legacy status

The July MarketFWv2 package is preserved as lineage. Its synthetic results remain P0-Q historical evidence, but the oscillator-first estimator is not inherited as authoritative because the audit found an observation-kernel inconsistency and insufficient refusal against simple alternatives.

## Next operations

1. Inventory the user's existing market data locally without uploading the corpus.
2. Profile representative files and identify data families, sampling intervals, timestamps, instruments, and available microstructure fields.
3. Expand the candidate native model family beyond AR0/AR1/AR2.
4. Build modal/vector outputs independently of χ.
5. Build multivariate conglomerate/network outputs.
6. Map Function and Limit surfaces on designated discovery data.
7. Freeze comparator tasks only after P0-Q closes sufficiently for P1.
